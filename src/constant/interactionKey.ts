import {fetchData, createKeyEntries} from 'util/dataHandler';

export const Action = createKeyEntries('actions');
export const Case = createKeyEntries('cases');
export const Mode = createKeyEntries('modes');